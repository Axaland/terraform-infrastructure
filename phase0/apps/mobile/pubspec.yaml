name: xxx_mobile_phase0
publish_to: "none"
description: "Shell Flutter per la Fase 0 dell'APP XXX"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

flutter:
  uses-material-design: true
