name: xxx_mobile
version: 0.1.0+1
description: "Shell app Flutter con onboarding e autenticazione federata per la Fase 1."
publish_to: "none"

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.5.1
  go_router: ^14.1.4
  dio: ^5.4.3
  package_info_plus: ^8.0.0
  shared_preferences: ^2.2.3
  firebase_crashlytics: ^4.0.4
  firebase_core: ^3.4.0
  app_links: ^3.5.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  build_runner: ^2.4.10

flutter:
  uses-material-design: true
  assets:
    - assets/config/
  generate: true
